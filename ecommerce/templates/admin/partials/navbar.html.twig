<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">Nanomania</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse ml-5" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item mr-3">
                <a class="nav-link" href="{{ path('admin_dashboard') }}">Dashboard</a>
            </li>
            <li class="nav-item mr-3">
                <a class="nav-link" href="{{ path('admin_user_index') }}">Utilisateurs</a>
            </li>
            <li class="nav-item mr-3">
                <a class="nav-link" href="{{ path('admin_product_index') }}">Jeux Vidéos</a>
            </li>
            <li class="nav-item mr-3">
                <a class="nav-link" href="#">Catégories</a>
            </li>
            <li class="nav-item mr-3">
                <a class="nav-link" href="{{ path('admin_platform_index') }}">Plateformes</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.firstName }}"> {{ app.user.firstName }}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
{#                        <a href="{{ path('product_create') }}" class="dropdown-item">Ajouter un jeu</a>#}
{#                        <div class="dropdown-divider"></div>#}
                        <a href="{{ path('account_index') }}" class="dropdown-item">Mon compte</a>
                        <div class="dropdown-divider"></div>
{#                        <a href="#" class="dropdown-item">Mes jeux en vente</a>#}
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon profil</a>
                        <a href="{{ path('account_password') }}" class="dropdown-item">Modifier mon mot de passe</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('account_register') }}" class="nav-link">S'inscrire</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('account_login') }}" class="nav-link">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}" role="alert">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}