{% extends 'base.html.twig' %}

{% block title %}Valider mon panier{% endblock %}

{% block body %}
    <div class="container">
        <div class="py-5 text-center">
            <h2>Validation du panier et moyen de paiement</h2>
            <p class="lead">Renseignez les informations demandées et saisissez votre moyen de paiement. Un PDF de votre facture ainsi qu'un code d'activation de votre jeu vous sera ensuite envoyé par mail.</p>
        </div>

        <div class="row">
            <div class="col-md-4 order-md-2 mb-4">
                <h4 class="d-flex justify-content-between align-items-center mb-3">
                    <span class="text-muted">Votre panier</span>
                    <span class="badge badge-secondary badge-pill">{{ panier.product | length }}</span>
                </h4>
                <ul class="list-group mb-3">
                    {% for product in panier.product %}
                        <li class="list-group-item d-flex justify-content-between lh-condensed">
                            <div>
                                <h6 class="my-0">{{ product.title|striptags|slice(0, 5)|raw }}</h6>
                                <small class="text-muted">{{ product.description }}</small>
                            </div>
                            <span class="text-muted">{{ product.price }}&euro;</span>
                        </li>
                    {% endfor %}
                    <li class="list-group-item d-flex justify-content-between">
                        <span>Total (&euro;)</span>
                        <strong>{{ panier.amount }}&euro;</strong>
                    </li>
                </ul>
            </div>

            <div class="col-md-8 order-md-1">
               {{ form_start(form) }}
               {{ form_widget(form) }}
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-check"></i>
                    Payer
                </button>
               {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
